# MiniPay 大支付购物平台开发文档

## 1. 文档目的

当前 MiniPay 项目是一个最小支付演示系统，主要覆盖“创建订单 -> 发起支付 -> 支付状态更新 -> 结果查询”的主链路。为了将项目升级为类似淘宝、京东等购物平台中的交易支付系统，需要在现有支付能力外，补全商品、用户、购物车、库存、订单、优惠、物流、退款等业务模块。

本文档用于整理后续改造需求、系统结构、模块职责、接口规划、数据库规划和分阶段开发计划。

## 2. 当前项目现状

### 2.1 当前技术栈

| 组件 | 当前技术 |
|------|----------|
| 网关 | Spring Cloud Gateway |
| 后端服务 | Spring Boot 3 |
| ORM | MyBatis-Plus |
| 数据库 | MySQL |
| 注册中心 | Nacos |
| 前端 | Vue 3 + Vite + Axios + Ant Design Vue |
| 构建工具 | Maven |
| 容器化 | Docker + Docker Compose |

### 2.2 当前模块结构

```text
MiniPay/
├── common/              # 公共模块，统一响应、异常、JWT、公共请求对象等
├── gateway/             # API 网关
├── order-service/       # 订单服务，目前主要支持创建订单和查询订单
├── payment-service/     # 支付服务，目前主要支持模拟支付和查询支付状态
├── frontend/            # 前端页面
├── sql/                 # 数据库脚本
├── docker-compose.yml   # 容器编排
├── init-db.sql          # 初始化数据库脚本
└── README.md            # 项目说明
```

### 2.3 当前业务链路

```text
用户输入金额和描述
  -> 创建订单
  -> 发起模拟支付
  -> 支付服务生成支付结果
  -> 查询订单和支付状态
```

当前系统的问题是：订单金额由用户手动输入，没有商品、SKU、购物车、库存、地址、优惠、物流、售后等真实购物平台要素。因此它更像支付功能 Demo，而不是完整交易平台。

## 3. 改造目标

将 MiniPay 从“支付演示系统”升级为“购物交易支付平台”，支持用户选择商品、加入购物车、确认订单、支付、查看订单状态，并为后续扩展优惠、物流、退款、商家后台等能力预留结构。

### 3.1 核心目标

1. 支持用户登录和身份识别。
2. 支持商品列表、商品详情、SKU 选择。
3. 支持加入购物车、修改数量、勾选结算。
4. 支持基于商品和数量创建订单，而不是手动输入金额。
5. 支持订单明细，记录购买的商品快照。
6. 支持库存校验、锁库存、扣库存、释放库存。
7. 支持支付单、支付状态流转、支付回调。
8. 支持订单状态流转，如待支付、已支付、已发货、已完成、已取消。
9. 支持后续扩展优惠券、退款、物流、商家管理。

### 3.2 目标业务主链路

```text
用户登录
  -> 浏览商品
  -> 查看商品详情
  -> 选择 SKU 和数量
  -> 加入购物车或立即购买
  -> 确认订单
  -> 选择收货地址
  -> 计算商品金额、优惠金额、运费、实付金额
  -> 校验并锁定库存
  -> 创建订单和订单明细
  -> 创建支付单
  -> 发起支付
  -> 支付成功回调
  -> 订单状态更新为已支付
  -> 扣减库存
  -> 商家发货
  -> 用户确认收货
  -> 订单完成
```

## 4. 目标系统结构

### 4.1 推荐微服务结构

```text
frontend
  |
gateway
  |
  |-- user-service          用户、登录、地址
  |-- product-service       商品、分类、SKU、价格
  |-- cart-service          购物车
  |-- inventory-service     库存、锁库存、扣库存、释放库存
  |-- order-service         订单、订单明细、订单状态流转
  |-- payment-service       支付单、支付渠道、支付回调、退款
  |-- promotion-service     优惠券、满减、活动，可后续添加
  |-- merchant-service      商家、店铺、结算账户，可后续添加
  |-- logistics-service     发货、物流、收货，可后续添加
  |-- message-service       消息通知、异步任务，可后续添加
```

### 4.2 建议项目目录结构

```text
MiniPay/
├── common/
├── gateway/
├── user-service/
├── product-service/
├── cart-service/
├── inventory-service/
├── order-service/
├── payment-service/
├── promotion-service/       # 第二阶段或第三阶段添加
├── logistics-service/       # 第三阶段添加
├── merchant-service/        # 第三阶段添加
├── frontend/
├── sql/
│   ├── minipay_user.sql
│   ├── minipay_product.sql
│   ├── minipay_cart.sql
│   ├── minipay_inventory.sql
│   ├── minipay_order.sql
│   ├── minipay_payment.sql
│   ├── minipay_promotion.sql
│   └── minipay_logistics.sql
├── docker-compose.yml
├── pom.xml
├── README.md
└── 大支付购物平台开发文档.md
```

## 5. 模块需求设计

## 5.1 common 公共模块

### 职责

公共模块用于沉淀多个服务都会使用的基础能力。

### 建议保留和完善内容

| 类型 | 内容 |
|------|------|
| 统一响应 | CommonResp |
| 统一异常 | MinipayException、MinipayExceptionEnum |
| 登录认证 | JwtUtil、用户上下文工具 |
| 请求对象 | 通用分页请求、通用状态更新请求 |
| 响应对象 | 通用分页响应 |
| 常量枚举 | 订单状态、支付状态、库存锁定状态、商品状态 |
| 工具类 | ID 生成、金额计算、时间工具 |

### 建议新增

```text
com.minipay.common.enums.OrderStatus
com.minipay.common.enums.PaymentStatus
com.minipay.common.enums.ProductStatus
com.minipay.common.enums.InventoryLockStatus
com.minipay.common.req.PageReq
com.minipay.common.resp.PageResp
com.minipay.common.context.UserContext
```

## 5.2 gateway 网关模块

### 职责

1. 统一入口。
2. 路由到各个后端服务。
3. 登录鉴权。
4. 跨域处理。
5. 请求日志。
6. 简单限流。

### 路由规划

| 路径 | 转发服务 |
|------|----------|
| /api/users/** | user-service |
| /api/products/** | product-service |
| /api/cart/** | cart-service |
| /api/inventory/** | inventory-service |
| /api/orders/** | order-service |
| /api/payments/** | payment-service |
| /api/promotions/** | promotion-service |
| /api/logistics/** | logistics-service |

## 5.3 user-service 用户模块

### 职责

1. 用户注册。
2. 用户登录。
3. JWT Token 签发。
4. 当前用户信息查询。
5. 收货地址管理。
6. 买家和卖家角色管理，后续扩展。

### 核心表

```text
user
user_address
user_account
```

### 核心接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/users/register | 用户注册 |
| POST | /api/users/login | 用户登录 |
| GET | /api/users/me | 查询当前用户 |
| GET | /api/users/addresses | 查询收货地址 |
| POST | /api/users/addresses | 新增收货地址 |
| PUT | /api/users/addresses/{id} | 修改收货地址 |
| DELETE | /api/users/addresses/{id} | 删除收货地址 |
| PUT | /api/users/addresses/{id}/default | 设置默认地址 |

## 5.4 product-service 商品模块

### 职责

1. 商品列表。
2. 商品详情。
3. 商品分类。
4. SKU 规格。
5. 商品价格。
6. 商品图片。
7. 商品上下架。
8. 商家发布商品，后续扩展。

### 核心表

```text
product
product_sku
product_category
product_image
```

### 核心接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/products | 商品列表，支持分页、分类、关键词 |
| GET | /api/products/{id} | 商品详情 |
| GET | /api/products/{id}/skus | 查询商品 SKU |
| POST | /api/products | 新增商品，商家或管理员使用 |
| PUT | /api/products/{id} | 修改商品 |
| PUT | /api/products/{id}/on-sale | 商品上架 |
| PUT | /api/products/{id}/off-sale | 商品下架 |

### 商品状态

```text
DRAFT       草稿
ON_SALE     上架
OFF_SALE    下架
DELETED     已删除
```

## 5.5 cart-service 购物车模块

### 职责

1. 加入购物车。
2. 修改商品数量。
3. 删除购物车商品。
4. 勾选或取消勾选商品。
5. 查询购物车列表。
6. 计算购物车小计。

### 核心表

```text
cart_item
```

### 核心接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/cart | 查询当前用户购物车 |
| POST | /api/cart/items | 加入购物车 |
| PUT | /api/cart/items/{id} | 修改数量 |
| DELETE | /api/cart/items/{id} | 删除购物车商品 |
| PUT | /api/cart/items/{id}/selected | 勾选或取消勾选 |
| DELETE | /api/cart/selected | 清空已选商品 |

## 5.6 inventory-service 库存模块

### 职责

1. 查询 SKU 库存。
2. 下单时锁定库存。
3. 支付成功后扣减库存。
4. 订单取消或支付超时后释放库存。
5. 防止超卖。

### 核心表

```text
inventory
inventory_lock
```

### 核心接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/inventory/skus/{skuId} | 查询 SKU 库存 |
| POST | /api/inventory/lock | 锁定库存 |
| POST | /api/inventory/deduct | 扣减库存 |
| POST | /api/inventory/release | 释放库存 |

### 库存处理规则

1. 创建订单前先校验库存。
2. 创建订单时锁定库存，不立即扣减真实库存。
3. 支付成功后扣减库存，并将库存锁记录改为已扣减。
4. 订单取消、支付失败或支付超时后释放库存。
5. 库存更新必须保证并发安全，可使用数据库乐观锁或 Redis 原子操作。

## 5.7 order-service 订单模块

### 当前问题

当前订单服务只有简单订单表，主要字段为金额、描述、订单号、状态。升级后，订单必须记录用户、地址、商品明细、价格明细、状态流转等信息。

### 职责

1. 确认订单。
2. 创建订单。
3. 保存订单商品快照。
4. 管理订单状态流转。
5. 查询订单列表和订单详情。
6. 取消订单。
7. 支付成功后更新订单状态。
8. 订单超时自动关闭。
9. 确认收货。

### 核心表

```text
orders
order_item
order_status_log
```

### 订单状态

```text
CREATED        已创建，待支付
PAYING         支付中
PAID           已支付，待发货
SHIPPED        已发货
RECEIVED       已收货
COMPLETED      已完成
CANCELLED      已取消
CLOSED         已关闭
REFUNDING      退款中
REFUNDED       已退款
```

### 核心接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/orders/confirm | 确认订单，计算价格和展示订单预览 |
| POST | /api/orders | 创建订单 |
| GET | /api/orders | 查询当前用户订单列表 |
| GET | /api/orders/{orderNo} | 查询订单详情 |
| POST | /api/orders/{orderNo}/cancel | 取消订单 |
| POST | /api/orders/{orderNo}/paid | 支付成功回调更新订单 |
| POST | /api/orders/{orderNo}/ship | 商家发货 |
| POST | /api/orders/{orderNo}/receive | 用户确认收货 |

### 创建订单请求示例

```json
{
  "addressId": 1,
  "items": [
    {
      "skuId": 1001,
      "quantity": 2
    }
  ],
  "couponId": null,
  "remark": "请尽快发货"
}
```

### 创建订单响应示例

```json
{
  "code": 200,
  "message": "创建订单成功",
  "success": true,
  "data": {
    "orderNo": "O202607230001",
    "totalAmount": 199.00,
    "discountAmount": 0.00,
    "freightAmount": 0.00,
    "payAmount": 199.00,
    "status": "CREATED"
  }
}
```

## 5.8 payment-service 支付模块

### 当前问题

当前支付服务主要是模拟支付结果。后续需要升级为围绕支付单、支付渠道、支付流水、回调和退款的独立支付模块。

### 职责

1. 创建支付单。
2. 发起支付。
3. 模拟支付渠道。
4. 查询支付状态。
5. 接收支付回调。
6. 保证支付幂等。
7. 支持退款。
8. 支付成功后通知订单服务。
9. 支付成功后触发库存扣减。

### 核心表

```text
payment_order
payment_flow
refund_order
```

### 支付状态

```text
WAITING       待支付
PROCESSING    支付处理中
SUCCESS       支付成功
FAILED        支付失败
CLOSED        已关闭
REFUNDING     退款中
REFUNDED      已退款
```

### 支付方式

```text
BALANCE       余额支付
ALIPAY_MOCK   模拟支付宝
WECHAT_MOCK   模拟微信支付
BANK_MOCK     模拟银行卡
```

### 核心接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/payments | 创建支付单并发起支付 |
| GET | /api/payments/{paymentNo} | 查询支付单 |
| GET | /api/payments/orders/{orderNo} | 根据订单号查询支付状态 |
| POST | /api/payments/callback/mock | 模拟支付回调 |
| POST | /api/payments/{paymentNo}/close | 关闭支付单 |
| POST | /api/payments/{paymentNo}/refund | 发起退款 |

### 发起支付请求示例

```json
{
  "orderNo": "O202607230001",
  "payAmount": 199.00,
  "payChannel": "ALIPAY_MOCK"
}
```

## 5.9 promotion-service 优惠模块

该模块可在第二阶段或第三阶段添加。

### 职责

1. 优惠券领取。
2. 优惠券使用。
3. 满减活动。
4. 商品活动价。
5. 下单价格计算。

### 核心表

```text
coupon
user_coupon
promotion_activity
promotion_product
```

## 5.10 logistics-service 物流模块

该模块可在第三阶段添加。

### 职责

1. 商家发货。
2. 物流单创建。
3. 物流状态查询。
4. 用户确认收货。

### 核心表

```text
logistics_order
logistics_trace
```

## 5.11 merchant-service 商家模块

该模块可在第三阶段添加。

### 职责

1. 商家入驻。
2. 店铺管理。
3. 商品发布。
4. 订单发货。
5. 商家结算账户。

### 核心表

```text
merchant
shop
merchant_account
settlement_order
```

## 6. 数据库规划

## 6.1 数据库拆分建议

第一阶段可以按服务拆库：

```text
minipay_user
minipay_product
minipay_cart
minipay_inventory
minipay_order
minipay_payment
```

第二、三阶段继续添加：

```text
minipay_promotion
minipay_logistics
minipay_merchant
```

## 6.2 核心表设计草案

### user

```sql
CREATE TABLE user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(64) NOT NULL UNIQUE,
  password VARCHAR(128) NOT NULL,
  nickname VARCHAR(64),
  phone VARCHAR(32),
  role VARCHAR(32) NOT NULL DEFAULT 'BUYER',
  status VARCHAR(32) NOT NULL DEFAULT 'NORMAL',
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL
);
```

### user_address

```sql
CREATE TABLE user_address (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  receiver_name VARCHAR(64) NOT NULL,
  receiver_phone VARCHAR(32) NOT NULL,
  province VARCHAR(64),
  city VARCHAR(64),
  district VARCHAR(64),
  detail_address VARCHAR(255) NOT NULL,
  is_default TINYINT NOT NULL DEFAULT 0,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL
);
```

### product

```sql
CREATE TABLE product (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  merchant_id BIGINT,
  category_id BIGINT,
  title VARCHAR(128) NOT NULL,
  description TEXT,
  main_image VARCHAR(255),
  status VARCHAR(32) NOT NULL DEFAULT 'ON_SALE',
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL
);
```

### product_sku

```sql
CREATE TABLE product_sku (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  product_id BIGINT NOT NULL,
  sku_name VARCHAR(128) NOT NULL,
  attributes_json VARCHAR(512),
  price DECIMAL(10, 2) NOT NULL,
  original_price DECIMAL(10, 2),
  status VARCHAR(32) NOT NULL DEFAULT 'ON_SALE',
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL
);
```

### cart_item

```sql
CREATE TABLE cart_item (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  product_id BIGINT NOT NULL,
  sku_id BIGINT NOT NULL,
  quantity INT NOT NULL,
  selected TINYINT NOT NULL DEFAULT 1,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL,
  UNIQUE KEY uk_user_sku (user_id, sku_id)
);
```

### inventory

```sql
CREATE TABLE inventory (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  sku_id BIGINT NOT NULL UNIQUE,
  total_stock INT NOT NULL DEFAULT 0,
  available_stock INT NOT NULL DEFAULT 0,
  locked_stock INT NOT NULL DEFAULT 0,
  version INT NOT NULL DEFAULT 0,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL
);
```

### inventory_lock

```sql
CREATE TABLE inventory_lock (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_no VARCHAR(64) NOT NULL,
  sku_id BIGINT NOT NULL,
  quantity INT NOT NULL,
  status VARCHAR(32) NOT NULL,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL,
  UNIQUE KEY uk_order_sku (order_no, sku_id)
);
```

### orders

```sql
CREATE TABLE orders (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_no VARCHAR(64) NOT NULL UNIQUE,
  user_id BIGINT NOT NULL,
  total_amount DECIMAL(10, 2) NOT NULL,
  discount_amount DECIMAL(10, 2) NOT NULL DEFAULT 0.00,
  freight_amount DECIMAL(10, 2) NOT NULL DEFAULT 0.00,
  pay_amount DECIMAL(10, 2) NOT NULL,
  status VARCHAR(32) NOT NULL,
  receiver_name VARCHAR(64),
  receiver_phone VARCHAR(32),
  receiver_address VARCHAR(255),
  remark VARCHAR(255),
  created_at DATETIME NOT NULL,
  paid_at DATETIME,
  cancelled_at DATETIME,
  completed_at DATETIME,
  updated_at DATETIME NOT NULL
);
```

### order_item

```sql
CREATE TABLE order_item (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_no VARCHAR(64) NOT NULL,
  product_id BIGINT NOT NULL,
  sku_id BIGINT NOT NULL,
  product_title VARCHAR(128) NOT NULL,
  sku_name VARCHAR(128),
  product_image VARCHAR(255),
  unit_price DECIMAL(10, 2) NOT NULL,
  quantity INT NOT NULL,
  total_amount DECIMAL(10, 2) NOT NULL,
  created_at DATETIME NOT NULL
);
```

### payment_order

```sql
CREATE TABLE payment_order (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  payment_no VARCHAR(64) NOT NULL UNIQUE,
  order_no VARCHAR(64) NOT NULL,
  user_id BIGINT NOT NULL,
  pay_amount DECIMAL(10, 2) NOT NULL,
  pay_channel VARCHAR(32) NOT NULL,
  status VARCHAR(32) NOT NULL,
  created_at DATETIME NOT NULL,
  paid_at DATETIME,
  closed_at DATETIME,
  updated_at DATETIME NOT NULL,
  UNIQUE KEY uk_order_no (order_no)
);
```

### payment_flow

```sql
CREATE TABLE payment_flow (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  payment_no VARCHAR(64) NOT NULL,
  order_no VARCHAR(64) NOT NULL,
  channel_trade_no VARCHAR(128),
  pay_channel VARCHAR(32) NOT NULL,
  amount DECIMAL(10, 2) NOT NULL,
  status VARCHAR(32) NOT NULL,
  request_body TEXT,
  response_body TEXT,
  created_at DATETIME NOT NULL
);
```

## 7. 服务间调用关系

### 7.1 创建订单调用关系

```text
frontend
  -> gateway
  -> order-service
      -> user-service 查询用户和地址
      -> product-service 查询商品和 SKU
      -> inventory-service 锁定库存
      -> order-service 保存订单和订单明细
```

### 7.2 支付成功调用关系

```text
frontend
  -> gateway
  -> payment-service 发起支付
      -> payment-service 更新支付单为 SUCCESS
      -> order-service 更新订单为 PAID
      -> inventory-service 扣减库存
      -> message-service 发送支付成功消息，可选
```

### 7.3 支付超时调用关系

```text
定时任务或 MQ 延迟消息
  -> order-service 关闭超时未支付订单
  -> payment-service 关闭支付单
  -> inventory-service 释放库存
```

## 8. 前端页面规划

### 8.1 买家端页面

| 页面 | 路由 | 功能 |
|------|------|------|
| 登录页 | /login | 用户登录 |
| 商品首页 | / | 商品推荐、分类入口 |
| 商品列表页 | /products | 商品分页、搜索、筛选 |
| 商品详情页 | /products/:id | 商品详情、SKU 选择、购买按钮 |
| 购物车页 | /cart | 购物车列表、勾选、修改数量 |
| 确认订单页 | /checkout | 地址选择、商品确认、价格确认 |
| 收银台页 | /pay/:orderNo | 选择支付方式并支付 |
| 支付结果页 | /pay/result | 展示支付结果 |
| 我的订单页 | /orders | 订单列表 |
| 订单详情页 | /orders/:orderNo | 订单详情、状态、商品明细 |
| 地址管理页 | /addresses | 新增、修改、删除地址 |

### 8.2 商家端页面，后续扩展

| 页面 | 路由 | 功能 |
|------|------|------|
| 商家后台首页 | /merchant | 经营概览 |
| 商品管理 | /merchant/products | 商品发布、编辑、上下架 |
| 订单管理 | /merchant/orders | 订单查询、发货 |
| 结算管理 | /merchant/settlements | 查看结算记录 |

## 9. 需要新增的基础组件

### 9.1 第一阶段建议新增

| 组件 | 用途 |
|------|------|
| Redis | 缓存商品、购物车、库存预扣、验证码、登录态辅助 |
| OpenFeign | 服务间 HTTP 调用 |
| Knife4j/Swagger | API 文档和接口调试 |
| Spring Security | 登录认证和接口鉴权 |

### 9.2 第二阶段建议新增

| 组件 | 用途 |
|------|------|
| RabbitMQ 或 RocketMQ | 支付成功通知、库存扣减、订单超时关闭 |
| Redisson | 分布式锁、库存并发控制 |
| Sentinel | 限流、熔断、降级 |

### 9.3 第三阶段建议新增

| 组件 | 用途 |
|------|------|
| Elasticsearch | 商品搜索 |
| MinIO | 商品图片、用户头像、支付凭证等文件存储 |
| Seata | 分布式事务，可按复杂度决定是否引入 |
| SkyWalking | 链路追踪和性能分析 |

## 10. 分阶段开发计划

## 10.1 第一阶段：购物支付 MVP

目标：完成“选择商品 -> 创建订单 -> 支付 -> 查询订单”的闭环。

### 后端任务

1. 新增 product-service。
2. 新增商品表和 SKU 表。
3. 新增商品列表、详情、SKU 查询接口。
4. 改造 order-service，支持订单主表和订单明细表。
5. 改造创建订单接口，由后端根据 SKU 和数量计算金额。
6. 改造 payment-service，支付请求基于 orderNo 创建支付单。
7. 支付成功后调用 order-service 更新订单状态。
8. 初始化测试商品数据。

### 前端任务

1. 新增商品列表页。
2. 新增商品详情页。
3. 新增确认订单页。
4. 新增收银台页。
5. 改造订单查询页，展示订单明细和支付状态。

### 第一阶段完成效果

```text
用户可以浏览商品，选择 SKU 和数量，创建订单，完成模拟支付，并查看订单状态。
```

## 10.2 第二阶段：购物车、库存和可靠交易

目标：解决真实交易中的购物车、库存并发和支付一致性问题。

### 后端任务

1. 新增 cart-service。
2. 新增 inventory-service。
3. 下单前调用库存服务锁库存。
4. 支付成功后扣减库存。
5. 订单取消或支付超时释放库存。
6. 引入 Redis 缓存和分布式锁。
7. 引入 MQ 处理支付成功通知和超时关单。
8. 增加支付幂等处理，防止重复支付。

### 前端任务

1. 新增购物车页面。
2. 支持购物车勾选结算。
3. 支持修改数量和删除商品。
4. 支持订单倒计时支付。

## 10.3 第三阶段：平台化能力

目标：让项目从购物支付系统扩展为平台型系统。

### 后端任务

1. 新增 promotion-service，支持优惠券和满减。
2. 新增 logistics-service，支持发货和物流状态。
3. 新增 merchant-service，支持商家、店铺、商品管理。
4. 新增退款和售后能力。
5. 引入 Elasticsearch 支持商品搜索。
6. 引入 MinIO 支持图片上传。
7. 增加后台管理功能。

### 前端任务

1. 新增商家后台。
2. 新增商品管理页面。
3. 新增发货管理页面。
4. 新增优惠券页面。
5. 新增退款售后页面。

## 11. 推荐开发优先级

建议不要一次性添加所有模块，优先按下面顺序推进：

```text
1. product-service：商品、SKU、商品详情
2. order-service：订单主表、订单明细、订单状态
3. frontend：商品列表、商品详情、确认订单、支付页
4. payment-service：支付单、支付回调、订单状态更新
5. user-service：登录、用户信息、地址管理
6. cart-service：购物车
7. inventory-service：锁库存、扣库存、释放库存
8. Redis + MQ：超时关单、异步通知、并发控制
9. promotion-service：优惠券、满减
10. logistics-service：发货、收货、物流
11. merchant-service：商家和店铺管理
```

## 12. 关键业务规则

### 12.1 金额计算规则

1. 前端不能直接决定订单最终金额。
2. 前端只传 skuId、quantity、addressId、couponId 等信息。
3. 后端根据商品价格、数量、优惠、运费计算订单金额。
4. 创建订单时保存商品价格快照，避免商品改价影响历史订单。

### 12.2 库存规则

1. 商品详情展示可售库存。
2. 创建订单时锁定库存。
3. 支付成功后扣减库存。
4. 支付失败、订单取消、支付超时后释放库存。
5. 并发下单时不能出现 available_stock 小于 0。

### 12.3 支付规则

1. 一个订单只能有一个有效支付单。
2. 重复发起支付时应返回已有支付单或进行幂等处理。
3. 支付成功回调可能重复到达，必须保证幂等。
4. 支付成功后才能将订单改为 PAID。
5. 支付金额必须与订单应付金额一致。

### 12.4 订单状态规则

1. CREATED 订单可以取消。
2. PAID 订单不能直接取消，需要走退款流程。
3. SHIPPED 订单可以确认收货。
4. COMPLETED 订单一般不能再修改。
5. REFUNDING 和 REFUNDED 状态由退款流程控制。

## 13. API 响应规范

所有后端接口建议统一使用 common 模块中的 CommonResp。

```json
{
  "code": 200,
  "message": "操作成功",
  "success": true,
  "data": {}
}
```

错误响应示例：

```json
{
  "code": 400,
  "message": "库存不足",
  "success": false,
  "data": null
}
```

## 14. 开发规范建议

1. 前端统一通过 gateway 调用后端，不直接访问具体服务端口。
2. 后端服务间调用优先使用 OpenFeign。
3. 所有接口统一返回 CommonResp。
4. 金额字段统一使用 BigDecimal，数据库使用 DECIMAL。
5. 状态字段统一使用枚举，不要散落字符串。
6. 订单号、支付单号、退款单号使用统一 ID 生成策略。
7. 创建订单、支付回调、扣减库存等核心接口必须考虑幂等。
8. 数据库脚本按服务拆分，放在 sql 目录。
9. 每个服务保留 health 接口，便于网关和部署检查。
10. 重要状态变更需要写状态日志，便于排查问题。

## 15. 近期可执行任务清单

### 第一批任务

- [ ] 新建 product-service 模块。
- [ ] 在父 pom.xml 中添加 product-service 模块。
- [ ] 新建 minipay_product 数据库脚本。
- [ ] 新建 product、product_sku、product_category、product_image 表。
- [ ] 实现商品列表接口。
- [ ] 实现商品详情接口。
- [ ] 实现 SKU 查询接口。
- [ ] 在 gateway 中添加商品服务路由。
- [ ] 前端新增商品列表页和商品详情页。

### 第二批任务

- [ ] 改造 order-service 数据库表。
- [ ] 新增 order_item 表。
- [ ] 创建订单接口改为接收 skuId 和 quantity。
- [ ] 创建订单时调用 product-service 查询商品信息。
- [ ] 创建订单时保存商品快照。
- [ ] 前端新增确认订单页。

### 第三批任务

- [ ] 改造 payment-service 支付单结构。
- [ ] 支付接口改为基于 orderNo 支付。
- [ ] 支付成功后更新订单状态。
- [ ] 增加支付幂等判断。
- [ ] 前端新增收银台页和支付结果页。

### 第四批任务

- [ ] 新建 user-service。
- [ ] 实现登录、注册、当前用户接口。
- [ ] 实现收货地址管理。
- [ ] 网关接入 JWT 鉴权。
- [ ] 前端新增登录页和地址管理页。

### 第五批任务

- [ ] 新建 cart-service。
- [ ] 实现购物车增删改查。
- [ ] 前端新增购物车页。
- [ ] 支持购物车勾选结算。

### 第六批任务

- [ ] 新建 inventory-service。
- [ ] 实现库存查询、锁库存、扣库存、释放库存。
- [ ] 下单接入库存锁定。
- [ ] 支付成功接入库存扣减。
- [ ] 订单取消和超时接入库存释放。

## 16. 总结

MiniPay 后续升级的核心方向不是单纯把 payment-service 写复杂，而是补齐完整交易域。商品、购物车、订单、库存、支付共同组成交易闭环，其中支付是关键节点，但不是唯一核心。

建议优先完成第一阶段 MVP：

```text
商品列表 -> 商品详情 -> 确认订单 -> 创建订单 -> 发起支付 -> 支付成功 -> 查询订单
```

完成这个闭环后，项目就会从“支付 Demo”升级为“购物支付平台雏形”，再逐步加入购物车、库存、优惠、物流、退款、商家后台等能力。
